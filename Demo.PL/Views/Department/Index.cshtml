@model IEnumerable<Department>
@{
    ViewData["Title"] = "AllDepartments";
    // string VData = (string)ViewData["Msg"];

    //   VData = ViewBag.Msg;
}


 


@* @if (@ViewData["Msg"] is not null)
{
     <div class="alert alert-success"> @ViewData["Msg"]</div>


} *@

@* <div class="alert alert-success"> @ViewData["Message"]</div>
<br />
<div class="alert alert-primary"> @ViewBag.Message</div>
 *@
@if (TempData["Message"] is not null)
{
   <div class="alert alert-success"> @TempData["Message"]</div>    
}

<a asp-action="Create" class="btn btn-success">Create New Department </a>

<br />
<br />

@if (Model.Count() > 0)
{  
    <h1> All Departments</h1>
    <br  />
    <br />

    <table class="table table-striped table-hover mt-3">
        <thead>
            <tr>
                <td>@Html.DisplayNameFor(D=>D.Code)</td>
                <td>@Html.DisplayNameFor(D=>D.Name)</td>
                <td>@Html.DisplayNameFor(D => D.CreationDate)</td>
                <td>Details</td>
                <td>Update</td>
                <td>Delete</td>

            </tr>

        </thead>

        <tbody>
            @foreach (var department in Model)
            { 
             <tr>
                    <td>@department.Code</td>
                    <a href="e:\course.net\mvc\assignment_session3_mvc\g03_demo_solution\demo.pl\views\department\create.cshtml"></a>
                    <td>@department.Name</td>
                    <td>@department.CreationDate</td>
                    <partial name="_ButtonParitialView" model="@department.Id"/>
             </tr>
            
            }

        </tbody>
    </table>
}
else {
    <div class="mt-5 alert alert-warning">
        <h3> There is No Employees</h3>
    </div>

}

